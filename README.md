# THU-Course-Select-Simple
简单的抢课脚本，包含两个部分：
- 进入选课界面时自动跳转至一级选课页面
- 自动抢课：点击按钮自动勾选包含关键词的课程并提交（需要手动在顶部确认）

目前只支持必修和限选选课
## 运行方式
由于 T大 选课网站是基于 iframe 的，不能直接在控制台运行脚本，需要使用篡改猴插件运行。

自动选课的关键词硬编码在脚本中，使用时请自行修改为需要的关键词。